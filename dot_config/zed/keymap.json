[
  // 1. Navegación entre Paneles (Sin afectar la Terminal)
  {
    "context": "Dock || Editor || ProjectPanel",
    "bindings": {
      "ctrl-h": "workspace::ActivatePaneLeft",
      "ctrl-l": "workspace::ActivatePaneRight",
      "ctrl-k": "workspace::ActivatePaneUp",
      "ctrl-j": "workspace::ActivatePaneDown"
    }
  },

  // 1b. Navegación desde la Terminal (Usando Ctrl+Alt para no romper Backspace/Enter)
  {
    "context": "Terminal",
    "bindings": {
      "ctrl-alt-h": "workspace::ActivatePaneLeft",
      "ctrl-alt-l": "workspace::ActivatePaneRight",
      "ctrl-alt-k": "workspace::ActivatePaneUp",
      "ctrl-alt-j": "workspace::ActivatePaneDown"
    }
  },

  // 2. Navegación y Toggle en el Explorador de Archivos (ProjectPanel)
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "space e": "workspace::ToggleLeftDock",
      "escape": "workspace::ToggleLeftDock",
      "q": "workspace::ToggleLeftDock",
      "j": "menu::SelectNext",
      "k": "menu::SelectPrev",
      "h": "project_panel::CollapseSelectedEntry",
      "l": "project_panel::ExpandSelectedEntry",
      "o": "project_panel::OpenPermanent",
      "a": "project_panel::NewFile",
      "A": "project_panel::NewDirectory",
      "r": "project_panel::Rename",
      "d": "project_panel::Delete"
    }
  },

  // 3. Modo Insert de Vim: Escape Rápido (Tu marca personal)
  {
    "context": "Editor && vim_mode == insert && !menu",
    "bindings": {
      "j k": "vim::NormalBefore",
      "j j": "vim::NormalBefore"
    }
  },

  // 4. Atajos Globales y Navegación de Buffers (Bracket Motions]b / [b)
  {
    "context": "Workspace",
    "bindings": {
      "alt-s": "zed::OpenSettingsFile",
      "alt-k": "zed::OpenKeymapFile",
      "ctrl-`": "terminal_panel::ToggleFocus",
      "] b": "pane::ActivateNextItem",
      "[ b": "pane::ActivatePrevItem"
    }
  },

  // 5. Modo Normal de Vim: Leader Key (Espacio) (Toggle Verdadero con workspace::ToggleLeftDock)
  {
    "context": "Editor && vim_mode == normal && !VimWaiting && !menu",
    "bindings": {
      // Explorador (Toggle de la barra lateral izquierda)
      "space e": "workspace::ToggleLeftDock",
      "space f f": "file_finder::Toggle",
      "space f p": "projects::OpenRecent",
      "space p a": "editor::CopyPath",
      "space b d": "pane::CloseActiveItem",

      // Mover líneas (Alt+j / Alt+k)
      "alt-j": "editor::MoveLineDown",
      "alt-k": "editor::MoveLineUp",

      // Git & Diffs
      "space g g": "git_panel::ToggleFocus",
      "space g h d": "editor::ToggleSelectedDiffHunks",

      // LSP & Acciones de Código
      "space c a": "editor::ToggleCodeActions",
      "space .": "editor::ToggleCodeActions",
      "space c r": "editor::Rename",
      "space o r": "editor::OrganizeImports",
      "g d": "editor::GoToDefinition",
      "g D": "editor::GoToDefinitionSplit",
      "g i": "editor::GoToImplementation",
      "g r": "editor::FindAllReferences",
      "] d": "editor::GoToDiagnostic",
      "[ d": "editor::GoToPreviousDiagnostic",
      "space t d": "editor::ToggleDiagnostics",

      // Alternar Ayudas Visuales
      "space t i": "editor::ToggleInlayHints",
      "space u w": "editor::ToggleSoftWrap",
      "space s b": "buffer_search::Deploy"
    }
  }
]
