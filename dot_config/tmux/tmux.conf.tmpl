# ==============================================================================
# TMUX UNIVERSAL CONFIG
# Fusión: Legacy 2024 + Reset Pro + Dotbar Theme
# ==============================================================================

# --- General Settings & Fixes ---
set-environment -g PATH "{{ .chezmoi.homeDir }}/.local/share/mise/shims:{{ .chezmoi.homeDir }}/.local/share/mise/bin:/usr/local/bin:/usr/bin:/bin"
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*:RGB"
set -ga terminal-overrides ",xterm-256color:RGB"
set -ga terminal-overrides ",xterm-kitty:RGB"
set -ga terminal-overrides ",tmux-256color:RGB"
set -g mouse on
set -g set-clipboard on
set -g detach-on-destroy off
set -g escape-time 0
set -g history-limit 1000000
set -g status-interval 3
set -g status-left-length 100
set -g status-right-length 100

# --- Indexing & Naming ---
set -g base-index 1
set -g pane-base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g automatic-rename off

# --- Keybindings TMUX ---
# General and System
# ctrl+b ? - Mostrar lista de todos los atajos activos
# ctrl+b : - Abrir cli de tmux
# ctrl+b d - Desacoplar (detach) la session actual
# ctrl+b D - Elegir un cliente (?) especifico para desacoplarlo.
# ctrl+b t - Mostrar reloj digital gigante
# ctrl+b [ - Entrar en modo copia
# ctrl+b ] - Pega el ultimo texto copiado
# Session Managment
# ctrl+b s - Abrir selector de sessions (sesionx ?)
# ctrl+b $ - Renombrar la session actual
# ctrl+b ( - Cambia a la session/cliente anterior
# ctrl+b ) - Cambia a la session/cliente siguiente
# ctrl+b L - Cambia a la ultima session utilizada
# Windows Managment
# ctrl+b c - Crear una nueva ventana (usar current path)
# ctrl+b , - Renombrar la ventana actual
# ctrl+b & - Cerrar (matar) la ventana actual, pide confirmacion
# ctrl+b n - Ir a la siguiente ventana
# ctrl+b p - Ir a la ventana anterior
# ctrl+b l - Ir a la ultima ventana activa
# ctrl+b w - Abrir el selector de ventanas y sessiones (choose-tree ?)
# ctrl+b [0 - 9] - Saltar directamente a la ventana con ese numero
# ctrl+b f - Busca texto en todas las ventanas abiertas
# ctrl+b . - Mover la ventana actual a un indice numerico especifico
# Windows Layouts
# ctrl+b Space - Cambia al siguiente layout (cicla entre horizontal y vertical)
# ctrl+b Alt+1 - Layout equitativo horizontal
# ctrl+b Alt+2 - Layout equitativo vertical
# ctrl+b Alt+3 - Layout principal horizontal, secundarios abajo
# ctrl+b Alt+4 - Layout principal vertical, secundarios abajo
# ctrl+b Alt+5 - Layout en rejilla (?)
# Panes Managment
# ctrl+b % - Split vertical |
# ctrl+b " - Split horizontal ---
# ctrl+b x - Cierra el panel actual, pide confirmacion
# ctrl+b z - Zoom en el panel actual
# ctrl+b o - Saltar al siguiente panel de la ventana
# ctrl+b ; - Salta al ultimo panel activo
# ctrl+b ! - Convierte el panel actual en una ventana independiente
# ctrl+b q - Muestra los numeros de los paneles, pulsar numero para enfocarlo
# ctrl+b { - Intercambia el panel actual con el anterior
# ctrl+b } - Intercambia el panel actual con el siguiente
# ctrl+b Flechas - Moverse al panel continuo
# ctrl+b ctrl+Flechas- Redimensionar el panel actual

# --- Betters Keybindings ---
bind r source-file "~/.config/tmux/tmux.conf" \; display-message "Config reloaded..."
bind c new-window -c "#{pane_current_path}"
bind '"' split-window -v -c '#{pane_current_path}'
bind % split-window -h -c '#{pane_current_path}'

# --- Image Preview Support ---
set -g allow-passthrough on
set -g visual-activity off
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

# --- Copy Mode (Vim Style) ---
setw -g mode-keys vi
set -s set-clipboard on                          # Habilitar soporte OSC 52 (Copia SSH)
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "wl-copy"

# Click derecho para pegar en Tmux (Solo si no hay menu activo)
unbind-key -n MouseDown3Pane
bind-key -n MouseDown3Pane run "wl-paste | tmux load-buffer - && tmux paste-buffer"

# --- Plugins (TPM) ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'vaaleyard/tmux-dotbar'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# --- Plugin Configuration ---
set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.config/tmux/plugins/'

# Resurrect & Continuum (Persistencia Indestructible)
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '10'

set -g @sessionx-bind 's'
set -g @sessionx-zoxide-mode 'on'

# --- Theme (Managed by flavours) ---
if-shell "test -f ~/.config/tmux/colors.conf" "source-file ~/.config/tmux/colors.conf"

# Theme (Dotbar) Customization
set -g @tmux-dotbar-position top
set -g @tmux-dotbar-bold-current-window true
set -g @tmux-dotbar-ssh-icon-only true
set -g @tmux-dotbar-right false

# Colores Predeterminados (Kanagawa Dragon)
set -g @tmux-dotbar-bg "#1d1c19"
set -g @tmux-dotbar-fg "#a6a69c"
set -g @tmux-dotbar-active-bg "#c4746e"        # Rojo (Seal) para ventana activa
set -g @tmux-dotbar-active-fg "#181616"
set -g @tmux-dotbar-prefix-bg "#c4746e"        # Rojo (Seal) para Prefix activo
set -g @tmux-dotbar-prefix-fg "#181616"

# Pane borders y estilos de selección (Kanagawa Dragon)
set -g pane-border-style "fg=#2a2828"
set -g pane-active-border-style "fg=#658594"  # Azul Pizarra para panel activo
set -g message-style "fg=#c8c093,bg=#1d1c19"   # Oro para comandos
set -g clock-mode-colour "#658594"
set -g mode-style "fg=#181616,bg=#658594"      # Azul Pizarra para selección

# --- Auto-install TPM & Plugins ---
if "test ! -d ~/.config/tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"

run '~/.config/tmux/plugins/tpm/tpm'

# Force SessionX bind to 's' after TPM loads
bind s run-shell "~/.config/tmux/plugins/tmux-sessionx/scripts/sessionx.sh"
