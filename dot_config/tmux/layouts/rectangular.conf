# ~/.config/tmux/layouts/rectangular.conf
# Layout Rectangular - Estilo Omarchy Clásico (Flat y Rectangular)

# Posición y alineación
set -g status-position top
set -g status-justify left

# Colores generales de la barra (transparente)
set -g status-style "bg=default,fg=default"

# Limpiar estilos heredados de plugins anteriores para asegurar transparencia
set -g window-status-style "bg=default,fg=#{@grey1}"
set -g window-status-current-style "bg=default,fg=#{@blue},bold"
set -g window-status-activity-style "bg=default"
set -g window-status-bell-style "bg=default"

# Lado Izquierdo: [Sesión]
# - Bloque rectangular sólido: azul en reposo, acento/rojo al presionar Prefix.
# - Removido el icono para dejarlo 100% fiel al Omarchy original.
set -g status-left-length 30
set -g status-left "#{?client_prefix,#[fg=#{@bg0} bg=#{@accent} bold] #S #[bg=default],#[fg=#{@bg0} bg=#{@blue} bold] #S #[bg=default]} "

# Lado Derecho: [Modos]
# - Muestra COPY, PREFIX o ZOOM de forma dinámica en color acento/azul.
set -g status-right-length 50
set -g status-right "#[fg=#{@blue}]#{?pane_in_mode,COPY ,}#{?client_prefix,PREFIX ,}#{?window_zoomed_flag,ZOOM ,}"

# Formato de Ventanas:
# - Sin separadores, solo texto plano plano.
# - Inactiva: Gris y sin fondo.
# - Activa: Azul/Acento en negrita y sin fondo.
setw -g window-status-separator ""
setw -g window-status-format "#[fg=#{@grey1}] #I:#W "
setw -g window-status-current-format "#[fg=#{@blue} bold] #I:#W "

