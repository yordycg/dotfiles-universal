# =============================================================================
# ~/.config/neovide/config.toml — Neovide Hardware GUI Settings
# Administrado por chezmoi
# =============================================================================

vsync = true

[box-drawing]
# Renderizado nativo por GPU para líneas y cajas de división (evita cortes verticales)
mode = "native"

[box-drawing.sizes]
default = [2, 4]
