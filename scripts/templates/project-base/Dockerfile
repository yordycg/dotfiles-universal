# Reemplazar con la imagen base adecuada (ej. python:3.12-slim, node:20-alpine)
FROM alpine:latest

WORKDIR /app

# Copiar el código fuente
COPY src/ .

# Comando por defecto (cambiar según el framework)
CMD ["echo", "Contenedor iniciado correctamente"]
