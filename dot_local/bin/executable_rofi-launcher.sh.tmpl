#!/usr/bin/env bash

# Rofi Launcher - Senior Edition
# Simple wrapper to trigger drun or other modes.

if pgrep -x "rofi" > /dev/null; then
    pkill rofi
    exit 0
fi

rofi -show drun -theme $HOME/.config/rofi/config.rasi
