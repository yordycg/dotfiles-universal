#!/usr/bin/env bash

# Rofi Clipboard - Senior Edition
# Searchable clipboard history using cliphist.

if pgrep -x "rofi" > /dev/null; then
    pkill rofi
    exit 0
fi

cliphist list | rofi -dmenu -p "󰅌 " -theme $HOME/.config/rofi/config.rasi | cliphist decode | wl-copy
